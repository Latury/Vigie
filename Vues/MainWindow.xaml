<!--
    ╔══════════════════════════════════════════════════════════════════════╗
    ║                          VIGIE                                       ║
    ║        Centre de maintenance logicielle intelligent                  ║
    ║                                                                      ║
    ║  Module : Vues                                                       ║
    ║  Fichier : MainWindow.xaml                                           ║
    ║                                                                      ║
    ║  Rôle :                                                              ║
    ║  Définit la fenêtre principale de l’application.                     ║
    ║                                                                      ║
    ║  Licence : MIT                                                       ║
    ║  Copyright © 2026 Flo Latury                                         ║
    ╚══════════════════════════════════════════════════════════════════════╝
-->

<Window
    x:Class="Vigie.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="using:Vigie.Infrastructure"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Vigie"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Vigie.VueModeles"
    Title="Vigie"
    mc:Ignorable="d">

    <!--  Effet visuel moderne Windows 11  -->
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <!--  Layout principal  -->
    <Grid>

        <Grid.ColumnDefinitions>
            <!--  Colonne gauche fixe  -->
            <ColumnDefinition Width="220" />

            <!--  Colonne droite prend tout l’espace restant  -->
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Barre gauche  -->
        <Grid Grid.Column="0" Background="#384454">

            <Grid.Resources>

                <Style x:Key="NavButtonStyle" TargetType="Button">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="Height" Value="44" />
                    <Setter Property="Margin" Value="20,0" />
                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                    <Setter Property="Padding" Value="12,0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Grid x:Name="RootGrid" Background="Transparent">
                                    <ContentPresenter Margin="12,0" VerticalAlignment="Center" />

                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal" />
                                            <VisualState x:Name="PointerOver">
                                                <Storyboard>
                                                    <ColorAnimation
                                                        Storyboard.TargetName="RootGrid"
                                                        Storyboard.TargetProperty="(Grid.Background).(SolidColorBrush.Color)"
                                                        To="#4F6D8C"
                                                        Duration="0" />
                                                </Storyboard>
                                            </VisualState>
                                            <VisualState x:Name="Pressed">
                                                <Storyboard>
                                                    <ColorAnimation
                                                        Storyboard.TargetName="RootGrid"
                                                        Storyboard.TargetProperty="(Grid.Background).(SolidColorBrush.Color)"
                                                        To="#2F3B4A"
                                                        Duration="0" />
                                                </Storyboard>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

            </Grid.Resources>

            <StackPanel
                Margin="0,40,0,0"
                VerticalAlignment="Top"
                Spacing="12">

                <Button Content="Accueil" Style="{StaticResource NavButtonStyle}" />
                <Button Content="Historique" Style="{StaticResource NavButtonStyle}" />
                <Button Content="Paramètres" Style="{StaticResource NavButtonStyle}" />
                <Button Content="À propos" Style="{StaticResource NavButtonStyle}" />

            </StackPanel>

        </Grid>

        <!--  Zone principale droite  -->
        <Grid Grid.Column="1" Background="#F4FAFF">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!--  Bloc titre  -->
                <RowDefinition Height="Auto" />
                <!--  Carte état  -->
                <RowDefinition Height="*" />
                <!--  Liste  -->
                <RowDefinition Height="Auto" />
                <!--  Bouton  -->
            </Grid.RowDefinitions>

            <!--  Bloc titre + dernier scan  -->
            <StackPanel
                Grid.Row="0"
                Margin="40,30,0,0"
                Spacing="4">

                <TextBlock
                    FontSize="24"
                    FontWeight="SemiBold"
                    Foreground="#1F2933"
                    Text="État du système" />

                <TextBlock
                    FontSize="14"
                    Foreground="#6B7280"
                    Text="{Binding DernierScanTexte}" />

            </StackPanel>

            <!--  Carte état système  -->
            <Border
                Grid.Row="1"
                Width="480"
                Height="200"
                Margin="0,20,0,20"
                HorizontalAlignment="Center"
                Background="#FFFFFF"
                CornerRadius="12">

                <Border.Shadow>
                    <ThemeShadow />
                </Border.Shadow>

                <StackPanel
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="16">

                    <!--  Icône avec halo  -->
                    <Grid>

                        <!--  Halo léger  -->
                        <Ellipse
                            Width="110"
                            Height="110"
                            Fill="#2E7D32"
                            Opacity="0.08" />

                        <!--  Cercle outline  -->
                        <Ellipse
                            Width="80"
                            Height="80"
                            Fill="Transparent"
                            Stroke="#2E7D32"
                            StrokeThickness="4" />

                    </Grid>

                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="20"
                        FontWeight="SemiBold"
                        Foreground="#1F2933"
                        Text="{Binding EtatSysteme}" />

                </StackPanel>

            </Border>

            <!--  LISTVIEW  -->
            <ListView
                Grid.Row="2"
                Margin="40,10,40,10"
                Background="Transparent"
                BorderThickness="0"
                ItemsSource="{Binding Logiciels}">

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="#1F2933" />
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8" ColumnSpacing="20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="120" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                FontWeight="SemiBold"
                                Foreground="#1F2933"
                                Text="{Binding Nom}" />

                            <TextBlock
                                Grid.Column="1"
                                Foreground="#374151"
                                Text="{Binding VersionActuelle}" />

                            <TextBlock
                                Grid.Column="2"
                                FontWeight="SemiBold"
                                Foreground="#2563EB"
                                Text="{Binding NouvelleVersion}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>

            <!--  Bouton principal  -->
            <Button
                Grid.Row="3"
                Width="220"
                Height="56"
                Margin="0,20,0,40"
                HorizontalAlignment="Center"
                Background="#384454"
                BorderThickness="0"
                Command="{Binding ScannerCommande}"
                Content="Scanner"
                Foreground="White">


                <Button.Resources>
                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#4F6D8C" />
                    <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#2F3B4A" />
                </Button.Resources>

            </Button>

        </Grid>

    </Grid>
</Window>